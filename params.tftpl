{
    apikey                    = ${apikey}
    cluster_name              = ${cluster_name}
    clusters                  = ${cluster_name}
    image_version             = ${image_version}
    secret_type               = ${secret_type}
}